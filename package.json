{
  "name": "nightwatch-dnshelper",
  "version": "1.0.0",
  "description": "Generate bash script for dnshelper",
  "main": "server/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nightwatch": "^0.8.18"
  },
  "dependencies": {
    "fs": "0.0.2"
  }
}
